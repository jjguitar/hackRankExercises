const PI = Math.PI;
    let r = 2.6;
    let perimeter
    // Print the area of the circle:
    let area = (PI * (Math.pow(2, r)));
    console.log(area);
    // Print the perimeter of the circle:
    perimeter = (2 * PI * r).toFixed(3);
    console.log(perimeter);